[core]
	longpaths = true
	editor = vim
[user]
	email = johnqwerty.lee1@gmail.com
	name = John Lee
[branch]
	sort = committerdate
[alias]
	kick = "!git commit --allow-empty -m \"kick pipe\" && git push"
	pushf = push --force-with-lease
	pushff = push -f
	unskip = update-index --no-skip-worktree
	skip = update-index --skip-worktree
	prdiff = "!f() { branch=${1:-HEAD}; git diff $(git merge-base master \"$branch\"); }; f"
	prlog = "!f() { branch=${1:-HEAD}; git log --oneline $(git merge-base master \"$branch\")..\"$branch\"; }; f"
	pradds = "!f() { branch=${1:-HEAD}; git diff --name-only --diff-filter=A $(git merge-base master \"$branch\"); }; f"
	prmods = "!f() { branch=${1:-HEAD}; git diff --name-only --diff-filter=M $(git merge-base master \"$branch\"); }; f"
	fzfcheckout = !git checkout $(git branch | fzf)
	ghclone = "!f() { git clone git@github.com:\"$1\"/\"$2\"; }; f"
	ghremoteadd = "!f() { git remote add \"$1\" git@github.com:\"$2\"/\"$3\"; }; f"
[push]
	autoSetupRemote = true
[http]
	postbuffer = 52428800
[rebase]
	autoStash = true
[safe]
	directory = /storage/emulated/0/notes/second-brain
	directory = /storage/emulated/0/notes/books
	directory = /storage/emulated/0/notes/music-sheets
	directory = /storage/emulated/0/notes/vansoftworks-technical-wiki
	directory = /storage/emulated/0/notes/notes-archive
	directory = /storage/emulated/0/notes/rubiks-cube-notes
	directory = D:/notes/vansoftworks-technical-wiki]
	directory = D:/notes/books
	directory = D:/notes/books
	directory = D:/notes/music-sheets
	directory = D:/notes/notes-archive
[fetch]
	writeCommitGraph = true
[init]
	defaultBranch = master
